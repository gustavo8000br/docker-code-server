---

# jenkins variables
project_name: docker-code-server
external_type: custom_json
release_type: prerelease
release_tag: development
ls_branch: development
repo_vars:
  - JSON_URL = 'https://api.github.com/repos/cdr/code-server/releases'
  - JSON_PATH = 'first(.[] | select(.prerelease == true)) | .tag_name'
  - CONTAINER_NAME = 'code-server'
  - BUILD_VERSION_ARG = 'CODE_RELEASE'
  - LS_USER = 'gustavo8000br'
  - LS_REPO = 'docker-code-server'
  - DOCKERHUB_IMAGE = 'gustavo8000br/docker-code-server'
  - DEV_DOCKERHUB_IMAGE = 'gustavo8000br/docker-code-server-dev'
  - PR_DOCKERHUB_IMAGE = 'gustavo8000br/docker-code-server-pr'
  - DIST_IMAGE = 'ubuntu'
  - MULTIARCH='true'
  - CI='false'
  - CI_WEB='true'
  - CI_PORT='8443'
  - CI_SSL='false'
  - CI_DELAY='120'
  - CI_DOCKERENV='TZ=US/Pacific'
  - CI_AUTH='user:password'
  - CI_WEBPATH=''
